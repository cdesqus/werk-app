<!DOCTYPE html>
<html>
<head>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap');
        
        body { font-family: 'Inter', sans-serif; color: #1f2937; margin: 0; padding: 40px; background: #fff; }
        .invoice-box { max-width: 800px; margin: auto; padding: 30px; border: 1px solid #e5e7eb; box-shadow: 0 0 10px rgba(0, 0, 0, .05); font-size: 14px; line-height: 24px; color: #555; }
        .header { display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #eee; padding-bottom: 20px; margin-bottom: 30px; }
        .logo { font-size: 24px; font-weight: 800; color: #111827; letter-spacing: -1px; }
        .logo sup { font-size: 10px; color: #10b981; margin-left: 2px; }
        .company-details { text-align: right; font-size: 12px; color: #6b7280; }
        
        .payslip-details { display: flex; justify-content: space-between; margin-bottom: 40px; }
        .employee-info h3 { margin: 0 0 5px; font-size: 16px; color: #111827; }
        .period-info { text-align: right; }
        .meta-label { font-size: 11px; text-transform: uppercase; color: #9ca3af; font-weight: 600; letter-spacing: 0.5px; }
        .meta-value { font-size: 14px; color: #374151; font-weight: 600; }

        .section-title { font-size: 12px; font-weight: 700; text-transform: uppercase; color: #9ca3af; margin-bottom: 10px; border-bottom: 1px solid #f3f4f6; padding-bottom: 5px; }
        
        table { width: 100%; text-align: left; border-collapse: collapse; margin-bottom: 30px; }
        th { padding: 10px 0; color: #6b7280; font-weight: 600; font-size: 12px; text-transform: uppercase; border-bottom: 1px solid #e5e7eb; }
        td { padding: 12px 0; border-bottom: 1px solid #f9fafb; font-size: 13px; color: #374151; }
        .amount-col { text-align: right; font-family: 'Inter', monospace; font-weight: 500; }
        .total-row td { border-top: 2px solid #e5e7eb; border-bottom: none; font-weight: 700; color: #111827; padding-top: 15px; font-size: 15px; }
        
        .net-pay-section { background: #f9fafb; padding: 20px; border-radius: 8px; text-align: right; margin-top: 40px; }
        .net-pay-label { font-size: 12px; font-weight: 600; color: #6b7280; text-transform: uppercase; margin-bottom: 5px; }
        .net-pay-amount { font-size: 32px; font-weight: 800; color: #10b981; letter-spacing: -1px; }
        
        .footer { margin-top: 50px; padding-top: 20px; border-top: 1px solid #eee; font-size: 11px; color: #9ca3af; text-align: center; }
        
        .badge { display: inline-block; padding: 2px 8px; border-radius: 4px; font-size: 10px; font-weight: 600; background: #ecfdf5; color: #047857; margin-left: 5px; }
    </style>
</head>
<body>
    <div class="invoice-box">
        <div class="header">
            <div class="logo">WERK<sup>IDE</sup></div>
            <div class="company-details">
                <strong>PT. WERK TECHNOLOGY INDONESIA</strong><br>
                Cyber 2 Tower, 18th Floor<br>
                Jl. H.R. Rasuna Said Blok X-5 No. 13<br>
                Jakarta Selatan, 12950<br>
                NPWP: 01.234.567.8-011.000
            </div>
        </div>

        <div class="payslip-details">
            <div class="employee-info">
                <div class="meta-label">Employee</div>
                <h3>{{user.name}} <span class="badge">{{user.role}}</span></h3>
                <div style="font-size: 12px; color: #6b7280;">ID: {{user.staffId}}</div>
                <div style="font-size: 12px; color: #6b7280;">Email: {{user.email}}</div>
            </div>
            <div class="period-info">
                <div class="meta-label">Pay Period</div>
                <div class="meta-value">{{periodStart}} - {{periodEnd}}</div>
                <div class="meta-label" style="margin-top: 10px;">Payment Date</div>
                <div class="meta-value">{{paymentDate}}</div>
            </div>
        </div>

        <div class="section-title">Earnings</div>
        <table>
            <thead>
                <tr>
                    <th>Description</th>
                    <th style="text-align: right;">Amount (IDR)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Basic Salary</td>
                    <td class="amount-col">{{formatCurrency calculations.baseSalary}}</td>
                </tr>
                <tr>
                    <td>Overtime <span style="font-size: 11px; color: #9ca3af;">({{calculations.totalOvertimeHours}} Hours)</span></td>
                    <td class="amount-col">{{formatCurrency calculations.overtimeTotal}}</td>
                </tr>
                <tr>
                    <td>Claims Reimbursement</td>
                    <td class="amount-col">{{formatCurrency calculations.claimTotal}}</td>
                </tr>
                {{#each earnings}}
                <tr>
                    <td>{{label}} <span class="badge">Adjustment</span></td>
                    <td class="amount-col">{{formatCurrency amount}}</td>
                </tr>
                {{/each}}
                <tr class="total-row">
                    <td>Total Earnings</td>
                    <td class="amount-col">{{formatCurrency calculations.grossPay}}</td>
                </tr>
            </tbody>
        </table>

        {{#if deductions.length}}
        <div class="section-title" style="margin-top: 30px;">Deductions</div>
        <table>
            <thead>
                <tr>
                    <th>Description</th>
                    <th style="text-align: right;">Amount (IDR)</th>
                </tr>
            </thead>
            <tbody>
                {{#each deductions}}
                <tr>
                    <td>{{label}}</td>
                    <td class="amount-col" style="color: #ef4444;">- {{formatCurrency amount}}</td>
                </tr>
                {{/each}}
                <tr class="total-row">
                    <td>Total Deductions</td>
                    <td class="amount-col" style="color: #ef4444;">- {{formatCurrency calculations.totalDeductions}}</td>
                </tr>
            </tbody>
        </table>
        {{/if}}

        <div class="net-pay-section">
            <div class="net-pay-label">Total Net Payable</div>
            <div class="net-pay-amount">{{formatCurrency calculations.netPay}}</div>
            <div style="font-size: 11px; color: #6b7280; margin-top: 5px;">Transferred to: {{user.bankDetails}}</div>
        </div>

        <div class="footer">
            Generated on {{generatedDate}} • ID: {{payslipId}}<br>
            This document is computer generated and valid without signature.
        </div>
    </div>
</body>
</html>
