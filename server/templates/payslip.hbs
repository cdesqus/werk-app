<!DOCTYPE html>
<html>

<head>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

        body {
            font-family: 'Inter', sans-serif;
            color: #1f2937;
            margin: 0;
            padding: 40px;
            background: #fff;
            -webkit-print-color-adjust: exact;
        }

        .invoice-box {
            max-width: 800px;
            margin: auto;
            padding: 40px;
            border: 1px solid #f3f4f6;
            box-shadow: 0 10px 30px rgba(0, 0, 0, .03);
            font-size: 14px;
            line-height: 24px;
            color: #555;
            border-radius: 12px;
            position: relative;
            overflow: hidden;
        }

        /* Decorative Top Bar */
        .invoice-box::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, #10b981 0%, #059669 100%);
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 50px;
            padding-top: 10px;
        }

        .logo {
            font-size: 28px;
            font-weight: 800;
            color: #111827;
            letter-spacing: -1px;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .logo span {
            color: #10b981;
        }

        .company-details {
            text-align: right;
            font-size: 13px;
            color: #6b7280;
            line-height: 1.6;
        }

        .company-title {
            font-weight: 700;
            color: #111827;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 4px;
        }

        .payslip-summary {
            display: flex;
            justify-content: space-between;
            background: #f9fafb;
            padding: 25px;
            border-radius: 12px;
            margin-bottom: 40px;
            border: 1px solid #e5e7eb;
        }

        .summary-col h3 {
            margin: 0 0 5px;
            font-size: 18px;
            color: #111827;
            font-weight: 700;
        }

        .summary-col p {
            margin: 0;
            font-size: 13px;
            color: #6b7280;
        }

        .summary-col .label {
            font-size: 11px;
            text-transform: uppercase;
            color: #9ca3af;
            font-weight: 600;
            letter-spacing: 0.5px;
            margin-bottom: 4px;
            display: block;
        }

        .section-title {
            font-size: 13px;
            font-weight: 700;
            text-transform: uppercase;
            color: #374151;
            margin-bottom: 15px;
            border-bottom: 2px solid #f3f4f6;
            padding-bottom: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        table {
            width: 100%;
            text-align: left;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        th {
            padding: 12px 15px;
            color: #6b7280;
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            background: #fafafa;
            border-radius: 6px;
        }

        td {
            padding: 15px;
            border-bottom: 1px solid #f3f4f6;
            font-size: 14px;
            color: #374151;
            font-weight: 500;
        }

        tr:last-child td {
            border-bottom: none;
        }

        .amount-col {
            text-align: right;
            font-family: 'Inter', monospace;
            font-weight: 600;
            color: #111827;
        }

        .deduction-text {
            color: #ef4444;
        }

        .total-row td {
            border-top: 2px solid #e5e7eb;
            font-weight: 800;
            color: #111827;
            font-size: 15px;
            background: #fafafa;
        }

        .net-pay-section {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            padding: 25px;
            border-radius: 12px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 40px;
            box-shadow: 0 10px 20px rgba(16, 185, 129, 0.2);
        }

        .net-pay-left .label {
            font-size: 13px;
            font-weight: 600;
            text-transform: uppercase;
            opacity: 0.9;
            margin-bottom: 4px;
        }

        .net-pay-left .bank-info {
            font-size: 12px;
            opacity: 0.8;
        }

        .net-pay-right {
            font-size: 36px;
            font-weight: 800;
            letter-spacing: -1px;
        }

        .footer {
            margin-top: 60px;
            padding-top: 25px;
            border-top: 1px solid #e5e7eb;
            font-size: 11px;
            color: #9ca3af;
            text-align: center;
            line-height: 1.6;
        }

        .badge {
            display: inline-flex;
            align-items: center;
            padding: 2px 8px;
            border-radius: 4px;
            font-size: 10px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .badge-role {
            background: #e0e7ff;
            color: #4338ca;
            margin-left: 8px;
            vertical-align: middle;
        }

        .badge-adj {
            background: #f3f4f6;
            color: #4b5563;
            font-size: 9px;
            margin-left: 5px;
        }

        .confidential-watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 100px;
            color: rgba(0, 0, 0, 0.03);
            font-weight: 900;
            text-transform: uppercase;
            pointer-events: none;
            z-index: 0;
        }
    </style>
</head>

<body>
    <div class="invoice-box">
        <div class="confidential-watermark">CONFIDENTIAL</div>

        <div class="header">
            <div class="logo">WERK<span>IDE</span></div>
            <div class="company-details">
                <div class="company-title">PT. IDE SOLUSI INTEGRASI</div>
                Gedung ASCOM<br>
                JL KH Abdullah Syafe'i no.23, Tebet<br>
                Jakarta Selatan 12920<br>
                Indonesia
            </div>
        </div>

        <div class="payslip-summary">
            <div class="summary-col">
                <span class="label">Employee Details</span>
                <h3>{{user.name}} <span class="badge badge-role">{{user.role}}</span></h3>
                <p>ID: <strong>{{user.staffId}}</strong></p>
                <p>Email: {{user.email}}</p>
            </div>
            <div class="summary-col" style="text-align: right;">
                <span class="label">Pay Period</span>
                <h3>{{periodStart}} &mdash; {{periodEnd}}</h3>
                <p>Payment Date: <strong>{{paymentDate}}</strong></p>
                <p>Ref: {{payslipId}}</p>
            </div>
        </div>

        <div class="section-title">Earnings</div>
        <table>
            <thead>
                <tr>
                    <th width="70%">Description</th>
                    <th width="30%" style="text-align: right;">Amount (IDR)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Basic Salary</td>
                    <td class="amount-col">{{formatCurrency calculations.baseSalary}}</td>
                </tr>
                <tr>
                    <td>Overtime <span class="badge badge-adj">{{calculations.totalOvertimeHours}} Hours</span></td>
                    <td class="amount-col">{{formatCurrency calculations.overtimeTotal}}</td>
                </tr>
                <tr>
                    <td>Claims Reimbursement</td>
                    <td class="amount-col">{{formatCurrency calculations.claimTotal}}</td>
                </tr>
                {{#each earnings}}
                <tr>
                    <td>{{label}} <span class="badge badge-adj">Adjustment</span></td>
                    <td class="amount-col">{{formatCurrency amount}}</td>
                </tr>
                {{/each}}
                <tr class="total-row">
                    <td>Total Earnings</td>
                    <td class="amount-col">{{formatCurrency calculations.grossPay}}</td>
                </tr>
            </tbody>
        </table>

        {{#if deductions.length}}
        <div class="section-title">Deductions</div>
        <table>
            <thead>
                <tr>
                    <th width="70%">Description</th>
                    <th width="30%" style="text-align: right;">Amount (IDR)</th>
                </tr>
            </thead>
            <tbody>
                {{#each deductions}}
                <tr>
                    <td>{{label}}</td>
                    <td class="amount-col deduction-text">- {{formatCurrency amount}}</td>
                </tr>
                {{/each}}
                <tr class="total-row">
                    <td>Total Deductions</td>
                    <td class="amount-col deduction-text">- {{formatCurrency calculations.totalDeductions}}</td>
                </tr>
            </tbody>
        </table>
        {{/if}}

        <div class="net-pay-section">
            <div class="net-pay-left">
                <div class="label">Total Net Payable</div>
                <div class="bank-info">Transferred via Bank Transfer</div>
                <div class="bank-info" style="margin-top: 4px; opacity: 0.7;">Account: {{#if
                    user.bankDetails}}{{user.bankDetails}}{{else}}On File{{/if}}</div>
            </div>
            <div class="net-pay-right">{{formatCurrency calculations.netPay}}</div>
        </div>

        <div class="footer">
            <p><strong>Strictly Private & Confidential</strong></p>
            Generated on {{generatedDate}} • Document ID: {{payslipId}}<br>
            This document is computer generated and is valid without a signature.
        </div>
    </div>
</body>

</html>